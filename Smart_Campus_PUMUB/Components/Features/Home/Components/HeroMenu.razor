<!-- HERO -->
<section id="hero" class="hero-section-custom">
    <div class="hero-bg">
        <img class="hero-bg-img" src="images/pu.jpg" alt="Polytechnic University (Maubin) Campus">
        <div class="hero-overlay-dark"></div>
    </div>

    <div class="hero-content-custom">
        <div class="hero-badge-pill">
            <i class="fas fa-star text-warning me-1"></i> EXCELLENCE IN TECHNOLOGY & ENGINEERING
        </div>

        <h1 class="hero-main-title">
            <span class="hero-title-main">Polytechnic University</span>
            <span class="hero-gold-accent">(Maubin)</span>
        </h1>

        <div class="hero-type-wrapper">
            <div class="typing-text" id="typingText"></div>
        </div>

        <p class="hero-main-desc">
            A premier institution of higher learning dedicated to advancing technology,
            engineering, and innovation in Myanmar. Shaping tomorrow's engineers and technologists since 2024.
        </p>
    </div>

    <div class="scroll-hint">
        <span>Scroll</span>
        <div class="scroll-dot"></div>
    </div>
</section>

<style>
    .hero-section-custom {
        position: relative;
        min-height: 100vh;
        height: auto;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        padding: 100px 1.5rem 60px;
        border: none !important;
        outline: none !important;
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        z-index: 0;
    }

    .hero-bg-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center 45%;
        filter: contrast(1.3) brightness(0.98);
    }

    .hero-overlay-dark {
        position: absolute;
        inset: 0;
        background: linear-gradient(180deg,
                rgba(10, 20, 45, 0.48) 0%,
                rgba(10, 20, 45, 0.28) 45%,
                rgba(8, 16, 36, 0.62) 100%);
        z-index: 1;
    }

    .hero-content-custom {
        position: relative;
        z-index: 2;
        max-width: 1100px;
        width: 100%;
        margin: 0 auto;
        text-align: center;
        border: none !important;
        outline: none !important;
        box-shadow: none !important;
        background: transparent !important;
    }

    /* ─── Frosted Badge Pill ─── */
    .hero-badge-pill {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(30, 58, 138, 0.85);
        border: 1px solid rgba(147, 197, 253, 0.6);
        color: #bfdbfe;
        padding: 8px 22px;
        border-radius: 999px;
        font-size: 0.82rem;
        font-weight: 700;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-bottom: 1.5rem;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
    }

    /* ─── Hero Title ─── */
    .hero-main-title {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 0.35rem;
        font-family: 'Playfair Display', 'Inter', Georgia, serif;
        font-size: clamp(2.2rem, 5.5vw, 4.4rem);
        font-weight: 900;
        color: #ffffff;
        line-height: 1.15;
        margin-bottom: 1.2rem;
        text-shadow: 0 4px 20px rgba(0, 0, 0, 0.95), 0 2px 6px rgba(0, 0, 0, 0.9);
        letter-spacing: -0.015em;
        border: none !important;
        outline: none !important;
        box-shadow: none !important;
        background: transparent !important;
    }

    .hero-title-main {
        display: block;
        color: #ffffff;
        white-space: normal;
        border: none !important;
        outline: none !important;
        box-shadow: none !important;
    }

    .hero-gold-accent {
        display: block;
        color: #f59e0b;
        text-shadow: 0 3px 18px rgba(0, 0, 0, 0.95), 0 0 30px rgba(245, 158, 11, 0.6);
        font-weight: 900;
        border: none !important;
        outline: none !important;
        box-shadow: none !important;
    }

    /* ─── Typing Animation ─── */
    .hero-type-wrapper {
        min-height: 2.6rem;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.2rem;
    }

    .typing-text {
        font-family: 'Playfair Display', serif;
        font-size: clamp(1.2rem, 2.8vw, 1.85rem);
        color: #38bdf8;
        font-weight: 600;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.95);
        margin: 0;
    }

    /* ─── Description ─── */
    .hero-main-desc {
        font-size: 1.15rem;
        color: #f8fafc;
        line-height: 1.85;
        margin-bottom: 2rem;
        max-width: 820px;
        margin-left: auto;
        margin-right: auto;
        font-weight: 500;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.95), 0 1px 3px rgba(0, 0, 0, 0.9);
    }
</style>